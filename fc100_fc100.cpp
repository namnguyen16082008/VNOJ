#include <bits/stdc++.h>
#define ll long long int

using namespace std;

string s;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    while (getline(cin,s)){
        if (s=="Free Contest 100"){
            cout<<"Let's start!"<<endl;
            return 0;
        }
        else cout<<"Wait"<<endl;
    }
}
